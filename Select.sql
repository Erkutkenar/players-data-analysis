-- select 100;

-- select first_name, actor_id,first_name from actor;
-- Use world;
-- Select Name, 2000 - IndepYear, Population, GNP, GNP/ Population * 1000 from country;

-- Select Concat(HeadofState, ' is the head of the state ',  name), Concat(Name,' ',HeadofState), Name,HeadofState, IndepYear from country;

-- Use sakila

-- select create_date,create_date + 1 from customer;

-- Use sakila

select first_name, last_name,CONCAT(first_name, ' ',last_name) as full_name from actor